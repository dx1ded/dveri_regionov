extends layouts/page
include _mixins

block config
  - page.title = "Монтаж - Дверной Регион"

block styles
  +style("/styles/cards-page.css")

include @cmps/Breadcrumbs/breadcrumbs

block partials
  include @cmps/Header/header
  main
    +breadcrumbs(
      { name: "Главная", path: "/" },
      { name: "Монтаж", path: "/installation" }
    )
    include @partials/installation/rooms
  include @cmps/Footer/footer

block scripts
  +script("/scripts/cards-page.js")
