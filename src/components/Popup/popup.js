export const popupMarkup = (text) => `
  <div class="popup">
    <button class="popup__open btn-reset btn--primary" aria-label="Открыть попап">?</button>
    <p class="popup__text">${text}</p>
  </div>
`
