extends layouts/page
include _mixins

block config
  - page.title = "Каталог - Дверной Регион"

block styles
  +style("styles/catalog.css")

include @cmps/Breadcrumbs/breadcrumbs

block partials
  include @cmps/Header/header
  main
    +breadcrumbs(
      { name: "Главная", path: "/" },
      { name: "Каталог", path: "/catalog" }
    )
    include @partials/catalog/types
  include @cmps/Footer/footer

block scripts
  +script("scripts/cards-page.js")
